#!/usr/bin/env python
import sys
import argparse
from project import Works

parser = argparse.ArgumentParser()

parser.add_argument("-rt", "--reftype", help="The type of reference you which to output. Options are 'bibtex' and 'ris'")
parser.add_argument("-doi", help="The type of reference you which to output. Options are 'bibtex' and 'ris'")

args = parser.parse_args()

if 
